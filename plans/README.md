# Implementation plans

Two batches share this index. Executors: read a plan fully before starting, honor its STOP conditions, and update your status row when done.

## Batch 2 — general audit (improve skill, commit `6a0cfa0`, 2026-07-15)

| # | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 004 | Make LLM extraction fail loudly + prevent overlapping monitor ticks | P1 | S | - | DONE (commit `4b1a799`) |
| 005 | Characterize dedup with tests, then fix UTC day-bucketing bug | P1 | M | - | DONE (commits `41af53f`, `b978e02`) |
| 006 | Constrain validation-command execution in the agent runner | P2 | M | - | DONE (commit `6b1d109`) |

Recommended order: 004 → 005 → 006. 004 and 005 both touch `apps/desktop/src/services/` (different files — parallelizable, but 004 is the smallest win). 006 is independent (Rust + one UI hint).

### Findings considered and rejected (don't re-audit)

- lucide-react "^1.24.0" version lag — resolves fine in `pnpm-lock.yaml`; CI builds pass.
- release.yml missing bun setup — already present (`oven-sh/setup-bun@v2`).
- `TodoList` whole-store Zustand subscription — that store holds only todos+tombstones; re-renders are legitimate.
- Dedup O(N×M) similarity cost — 30 s cadence, small N; profile before optimizing.
- `packages/cli`, `packages/core`, `apps/ocr-sidecar` "dead packages" — untracked local build dirt, not in git.
- `material/` in repo — 3 small logo PNGs; fine.
- Go server default DB password / TOCTOU races — server is an unwired in-memory stub (SyncService never instantiated); fold into the sync decision below if that feature is built.
- OCR engine Mutex contention — 30 s cadence is not a hot path.

### Unplanned findings still open (selected against, or awaiting a decision)

- API key stored plaintext (tauri-store JSON + `~/.taskly/pi-agent/models.json`) — audit finding #6, not selected.
- ExecutionStore re-render cascade (`store/index.ts:391–457`) — finding #7, not selected.
- No ESLint/Prettier for TS; no AGENTS.md — findings #8/#9, not selected.
- God files (`agent.rs` 1627, `AgentChatPane.tsx` 1146, `Settings.tsx` 1116, `store/index.ts` 871) — finding #10.
- Dead `packages/ocr-engine` (legacy Python sidecar) — finding #11, trivial removal.
- **Direction**: cloud sync is promised in README but unwired end-to-end (no auth, in-memory server, `SyncService` never called) — build properly or cut the claim; decide before shipping.

---

## Batch 1 — Animation plans

Generated by `improve-animations` against commit `37c3a0a`.

## Plan list

| # | Title | Severity | Status | Depends on |
| --- | --- | --- | --- | --- |
| 001 | Replace `transition-all` in high-frequency primitives | HIGH | DONE | - |
| 002 | Replace global reduced-motion kill switch with scoped policy | HIGH | DONE | 001 (recommended first for shared motion behavior) |
| 003 | Remove layout-driven sidebar width animation | MEDIUM | DONE | - |

## Recommended execution order

1. **001** — removes broad high-frequency perf risk in core primitives.
2. **002** — fixes accessibility policy and avoids over-suppressed feedback in reduced-motion mode.
3. **003** — removes layout-property animation from sidebar collapse/expand.

## Notes

- 001 and 003 are technically independent, but run 001 first to stabilize shared motion behavior before tuning page-level interactions.
- Re-run `improve-animations reconcile` after implementation to refresh statuses and line references.
